import { DashboardWrapper } from "@/components/features/layout/DashboardWrapper.tsx";
import { Button } from "@/components/ui/button.tsx";

export const PatientAssistance = () => {
    return (
        <DashboardWrapper>
            <Button variant={"themed"}>Assistance</Button>
        </DashboardWrapper>
    );
};
